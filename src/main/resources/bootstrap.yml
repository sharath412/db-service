spring:
  application:
    name: db-service
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: false
      discovery:
        enabled: true
        prefer-ip-address: true
