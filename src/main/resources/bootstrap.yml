spring:
  application:
    name: db-service
  cloud:
    consul:
      host: consul
      port: 8500
      config:
        enabled: false
      discovery:
        enabled: true
        prefer-ip-address: true
